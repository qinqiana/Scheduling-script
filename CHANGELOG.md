# 更新日志

当前版本 **1.9.4**。没有 1.1。

## 1.9.4（2026-09-10）

- 修复晚班加班导出再导入变成早班：导出区分「早加」「晚加」，导入保留原班次。
- 官方节假日只在首次写入，手工改过的节日条目不再被启动覆盖。
- data.db 损坏或完整性校验失败时自动回退到 .bak 备份。
- 月历写操作加请求序号守卫，编辑抽屉按钮在保存中禁用，连点不再旧响应覆盖新状态。
- 排班引擎内部重构：可行性判定收成单入口（force 路径补上次日锁早检查）、覆盖判定单源、校验规则表化、晚班计数收口、热路径优化、生成路径脱离数据库回落。排班结果由黄金快照逐字节锁定，四个黄金样例硬冲突 5、10、10、11 条。

## 1.9.2（2026-09-06）

- 后台生成排班，显示尝试进度并支持取消；生成期间的后续修改会使旧结果失效，不覆盖新数据。
- 增加输入矛盾诊断、独立业务不变量检查和已知可行样例；现有黄金样例的 35 条硬冲突均会归类展示，未将其误报为无解。
- 优化班次段修复的覆盖检查；与 1.9 的第二夹心休规则合并后，四个黄金样例硬冲突为 5、10、10、10 条。
- 统一单格、导入和清空时的请假、想休、加班、补休状态转换；补休与加班不会被“清空未锁定”误删。
- 同一数据目录只允许一个服务实例，重复启动会打开已有服务；绿色包沿用该行为。
- 月历折叠长规则说明，并可按类别查看并定位人员相关硬冲突。

## 1.9.1（2026-09-05）

- 批量写入增加事务回滚，数据库改为临时文件替换，恢复前校验备份并保留原库。
- 补充接口参数校验、统一 JSON 错误处理和同源访问限制。
- 修复 Excel 导入加班未锁定、月份切换旧响应覆盖及统计加载错误。
- 删除生成和导入中的重复落盘，构建增加前后端及 Vite 配置类型检查。
- 绿色包补齐 Excel 模板，修正文档中的打包命令。
- 新增审计回归检查，23 项测试通过；排班算法和原有黄金快照保持不变。

## 1.9（2026-09-01）

规则与界面：

- 覆盖不够时允许第 2 个夹心休；生成仍尽量每人每月 1 天。想休锁定的、月末覆盖逼出来的仍不计入。
- 月初月末不够一周的周末仍要值班，但不计入每人应出勤。
- 去掉「重排未锁定」。锁定格子生成时本来就不会改，该按钮与「生成」走同一套逻辑。

生成与测试：

- 固定 seed 的黄金快照测试，生成可脱离数据库复现。
- 引擎用 Ctx 和 Pass 数组编排流水线；seed 只在引擎内兜底。

## 1.8（2026-08-28）

- 可导入考勤表；导出按导入模板填写。
- 班次尽量集中：每人每月最多切 1 次。假日加班不计入、不查晚接早。
- 夹心休每人每月最多 1 天，并休优先于班次集中。
- 月末三天改为每月最后 3 个非法定假工作日。
- 日历显示连班天数。

## 1.7（2026-08-21）

- 放假按国办通告写入，再按《全国年节及纪念日放假办法》拆成两类：法定节假日全员休息、不值班；通告连休日按周末值班，不算法定工作日。调休上班日算法定工作日。
- 2024–2026 年日历以中国政府网刊登的国办发明电为准。
- 无请假、无手工加班/补休时，每人当月出勤必须等于该月法定工作日。
- 满自然周默认 5 上 2 休；和其他硬约束冲突时可以改成 4 上或 6 上，多出来的日期不再记加班。
- 生成时换种子重排，尽量做到硬约束为 0。

## 1.6（2026-08-20）

- 禁止隔天单班（不要工作一天休息一天）；法定节假日夹在中间的不算。
- 含加班也不能连上 7 天；连续 6 天后至少再休 2 天。
- 除月末最后三天外，每组每天早晚班尽量平均（软约束）。
- 增加「重排未锁定」：锁定格子不动，其余另排一套。（1.9 已去掉）

## 1.5（2026-08-20）

- 界面改为蓝色风格。
- 支持全部清空，以及单格清空。
- 格子上预先标的加班，生成时锁定，不再被改掉。
- 导出 Excel 按当月实际天数裁列。

## 1.4（2026-08-20）

- 月末最后三天（法定假日除外）每组多排人、最多休 1 人，且至少 2 个晚班在岗。
- 格子可标「加班」或「补休」，分别把当月应出勤 +1 或 −1。
- 精简生成逻辑和启动流程。

## 1.3（2026-08-20）

- 法定假按当时 13 天全体公民假日休息。
- 出勤对齐当月法定工作日。
- 满周默认 5 上 2 休，和其他硬约束冲突时可以多排。
- 同组每人每月晚班数量相差不超过 3 天。

## 1.2（2026-08-19）

- 按法定工作日和硬约束生成，尽量排出可执行的班表。
- 支持登记「想休」，生成时优先排休。

## 1.0（2026-08-18）

- 初版：按组生成早 / 晚 / 休班表。
- 支持导出考勤表。
